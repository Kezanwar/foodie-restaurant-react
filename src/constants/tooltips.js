import { MAX_IMAGE } from './files';

export const auth_tooltips = {
  password_requirements: {
    tooltip:
      'Password must include a lowercase letter, a uppercase letter, a special character and a number.',
    text: 'Password requirements'
  }
};

export const image_tooltip = {
  tooltip: `Image size is max ${MAX_IMAGE.text}.`,
  text: 'Image requirements'
};
