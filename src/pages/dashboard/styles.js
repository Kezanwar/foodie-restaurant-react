import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const DashboardTitleContainer = styled(Box)(({ theme }) => ({
  marginBottom: theme.spacing(4)
  //   [theme.breakpoints.up(999)]: { maxWidth: '50%' }
}));
